apiVersion: v1
kind: Service
metadata:
  name: statichtml-service
  namespace: default
spec:
  type: NodePort
  selector:
    app: statichtml
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080